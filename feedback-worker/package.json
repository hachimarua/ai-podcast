{
  "name": "ai-radio-feedback-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "db:migrate:local": "wrangler d1 migrations apply AI_RADIO_FEEDBACK_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply AI_RADIO_FEEDBACK_DB --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
